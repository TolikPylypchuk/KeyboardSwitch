<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l="clr-namespace:KeyboardSwitch.Settings.Localization"
             x:Class="KeyboardSwitch.Settings.Views.PreferencesView">
    <DockPanel>
        <StackPanel Orientation="Horizontal" Margin="10,10,0,0" DockPanel.Dock="Top">
            <CheckBox x:Name="InstantSwitchingCheckBox" Content="{l:Translate InstantSwitching}"
                      Classes="FormControl" />
            <CheckBox x:Name="SwitchLayoutCheckBox" Content="{l:Translate SwitchLayout}"
                      Classes="FormControl" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10,10,0,0" DockPanel.Dock="Top">
            <TextBlock Text="{l:Translate SwitchMode}" Classes="Label" />
            <ComboBox x:Name="SwitchModeComboBox" Classes="FormControl" />
        </StackPanel>

        <StackPanel x:Name="ActionPanel">
            <Button x:Name="SaveButton" Content="{l:Translate Save}" Classes="Primary" />
            <Button x:Name="CancelButton" Content="{l:Translate Cancel}" />
        </StackPanel>

        <ContentControl x:Name="PreferencesContent" />
    </DockPanel>
</UserControl>
